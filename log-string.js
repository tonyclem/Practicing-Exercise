// Declear a variable myString and assign to it, your Fullname, including spaces as the content of the strings

let myString = "Clement Sunday";

console.log(
  "here im declearing my myString and i'm assign my fullname as a values to it so let see what the outcome will looks like:"
);

// Now let comsole log myString and see what the outcome looks likes
console.log(myString);

// here we want to reassign myString with new variables
myString = "I'm a web developer";

// now let see what's the  outcome of myString
console.log("now let see what's the outcome of myString is:");

console.log(myString);
