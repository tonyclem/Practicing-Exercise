// Here we decleared a variable called mySentence

let mySentence = "Programming is so interesting!";

// we use length to find the length of any sentence or words
console.log(mySentence.length); // 30
