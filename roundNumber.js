// Here i am declearing a new variable called x and im assigning a value to it 7.25

let z = 7.25;

console.log(
  "Here i declear a new variable called z, eventually that has 7.25 as a value"
);

// function returns the largest integer less than or equal to a given number.
let a = Math.floor(7.25);

// Write a statement in which you log the a value
console.log(
  "I other for us, to get a value, to the nearest integar value, we need to use math Floor to do that calculation for us"
);

if (a > z) {
  console.log(true);
} else {
  console.log(false);
}
